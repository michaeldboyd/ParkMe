import { React, Component } from "react";
import {Text} from "react-native";

export default class Toolbar extends Component {
  render() {
    return (
        <Text>This is obviously not ready</Text>
    );
  }
}